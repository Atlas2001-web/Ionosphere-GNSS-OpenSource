# PyTECGg · 多星座 TEC 重建与校准（Python + Rust）

> [`PROJECTS.json` → `PyTECGg`](../../PROJECTS.json)  
> 上游：<https://github.com/viventriglia/PyTECGg>  
> 文档：<https://viventriglia.github.io/PyTECGg/>  
> **作者：viventriglia 等（不是本索引维护者自有项目）**；维护者仅 fork/星标关系见目录  
> 许可：GPLv3 · `pip install pytecgg` · 列表：[01 电离层](../../lists/01-ionosphere.md)

---

## 1. 场景

从 RINEX 观测/导航出发，做几何无关组合、弧段整平、偏差估计与校准，输出校准 sTEC/vTEC 与站天顶 **veq** 等，结果留在 **Polars DataFrame**。适合可编程批处理与多星座（G/E/C/R），RINEX 2/3/4、Hatanaka。

**何时用：** RINEX → 有绝对意义的 TEC 时序/IPP。  
**何时不用：** 只探活 → georinex；ROTI 一体机 → IonoMoni；全球球谐求解 → 其他 GIM。

**务必引用上游作者，勿与维护者 MATLAB SH-GIM 混淆。**

---

## 2. 概念

| 术语 | 人话 |
|---|---|
| GNSSContext | 接收机坐标、IPP 高度、星座等上下文 |
| 几何无关组合 | 消几何，得电离层相关观测量 |
| 弧段整平 | 相位对齐伪距，抑制噪声但引偏差 |
| 校准（Ciraolo 类） | 估弧段/硬件偏差，得绝对 sTEC |
| MoDip | 修正磁倾纬，建模空间变化 |
| veq | vertical equivalent |
| h_ipp | 薄壳高度（米；常用 350 km） |

---

## 3. 安装

官方标明 **Python 3.11–3.13**（以当前文档为准）；多数平台有 wheel。

```bash
python3 -m venv .venv && source .venv/bin/activate
python3 -m pip install -U pip
python3 -m pip install pytecgg
```

源码编译需 Rust 工具链。Docker Batch 见上游。

---

## 4. 逐步例子 + 输出

### 4.1 读入与 Context

```python
from pathlib import Path
from pytecgg.parsing import read_rinex_nav, read_rinex_obs
from pytecgg import GNSSContext

NAV_PATH = Path("BRDC00IGS_R_20240910000_01D_MN.rnx")
OBS_PATH = Path("YOUR00XXX_R_20240910000_01D_30S_MO.rnx")

nav_dict = read_rinex_nav(NAV_PATH)
df_obs, rec_pos, rinex_version = read_rinex_obs(OBS_PATH)
rec_name = OBS_PATH.name[:4].lower()

ctx = GNSSContext(
    receiver_pos=rec_pos,
    receiver_name=rec_name,
    rinex_version=rinex_version,
    h_ipp=350_000,
    systems=["G", "E"],
)
```

**预期：** ctx 系统归一为符号；h_ipp 出常用范围会 UserWarning。

### 4.2 预处理链（官方 quickstart 思路）

```python
from pytecgg.satellites import prepare_ephemeris, satellite_coordinates, calculate_ipp
from pytecgg.linear_combinations import calculate_linear_combinations
from pytecgg.tec_calibration import (
    extract_arcs, calculate_tec, calculate_vertical_equivalent,
)

ephem_dict = prepare_ephemeris(nav_dict, ctx)
# 以下变量名按你实际合并观测的 DataFrame 调整；见上游 0→2 文档
df_lc = calculate_linear_combinations(df_obs, ctx, selection_mode="availability")
df_coords = satellite_coordinates(df_lc["sv"], df_lc["epoch"], ephem_dict)
df_arcs = extract_arcs(df_lc, ctx, min_arc_length=120).join(
    df_coords, on=["sv", "epoch"], how="left"
)
df_geom = calculate_ipp(df_arcs, ctx, min_elevation=20)
```

**预期：** 含 IPP、高度角、弧段 id 的表。

### 4.3 校准

```python
df_calibrated = calculate_tec(
    df_geom, ctx=ctx, max_polynomial_degree=3, batch_size_epochs=30,
)
df_veq = calculate_vertical_equivalent(
    df_calibrated, ctx=ctx, max_polynomial_degree=3, batch_size_epochs=30,
)
# 关注列：stec, vtec, veq, bias, ele, azi, ...
print(df_calibrated.columns)
print(df_calibrated.select(["stec", "vtec"]).describe())
```

**预期：** stec/vtec 列出现；未校准相对量与校准后量级叙事不同。

### 4.4 导出

```python
df_calibrated.write_parquet("tec_calibrated.parquet")
df_calibrated.write_csv("tec_calibrated.csv")
```

### 4.5 与 IONEX 对照

同日读 GIM（[ionex-gim](./ionex-gim.md)），比较 VTEC 量级与日变化，不要求像素级一致。

### 4.6 与 IonoMoni 对照

同站日 STEC/ROTI 双视角。

---

## 5. 格式

输入：RINEX OBS/NAV（含压缩）、GNSSContext。  
输出：Polars DataFrame（stec/vtec/veq/bias/…）。  
辅助：文档提供下载工具函数思路。

---

## 6. 接到哪步

[02](../tutorials/02-gnss-dualfreq-tec.md)·[09](../tutorials/09-dcb-biases-deep.md)·[16](../tutorials/16-practice-one-day-tec.md)  
前处理 QC：georinex/Anubis  
网格对照：ionex-gim  
不规则体：IonoMoni  

路径 A 核心站。

---

## 7. 常见坑（≥12）

1. 把未校准当绝对 TEC。  
2. Python 版本无 wheel。  
3. GPLv3 合规。  
4. h_ipp/多项式/batch 乱拧。  
5. 作者归属混淆 SH-GIM。  
6. 弧段太短被剔光。  
7. 截止高度角过低。  
8. 导航星历不覆盖。  
9. 只跑 GPS 却与四系统 GIM 硬比。  
10. Polars/pandas 混用不熟。  
11. Hatanaka 依赖缺失。  
12. Docker 与本地路径。  
13. 磁暴日苛责算法。  
14. 接收机坐标错。  
15. 频率选择与系统不一致。  
16. 忘了 cite 上游。

---

## 8. 同类选型

| 需求 | 选 |
|---|---|
| Python+Rust 可编程校准 | **PyTECGg（viventriglia）** |
| C++ 监测 ROTI/AATR | IonoMoni |
| 仅读盘 | georinex |
| 官方 GIM | ionex + CDDIS |
| 维护者球谐仓 | SH-GIM（短述/边界） |

---

## 9. 长附录

官方文档路径：0 data ingestion → 1 preprocessing → 2 calibration → 3 batch。  
引用与 issue 去 viventriglia/PyTECGg。  
练习：改 h_ipp；改 max_polynomial_degree；G vs G+E；与 IONEX RMSE。


## 附录 · 扩展 FAQ 与教学展开

### F.1 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.2 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.3 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.4 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.5 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.6 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.7 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.8 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.9 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.10 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.11 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.12 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.13 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.14 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.15 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.16 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.17 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.18 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.19 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.20 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.21 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.22 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.23 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.24 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.25 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.26 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.27 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.28 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.29 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.30 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.31 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.32 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.33 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.34 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.35 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.36 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.37 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.38 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.39 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.40 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.41 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.42 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.43 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.44 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.45 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.46 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.47 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.48 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.49 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.50 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.51 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.52 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.53 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.54 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.55 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.56 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.57 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.58 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.59 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.60 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.61 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.62 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.63 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.64 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.65 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.66 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.67 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.68 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.69 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.70 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.71 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.72 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.73 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.74 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.75 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.76 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.77 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.78 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.79 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.80 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.81 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.82 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.83 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.84 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.85 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.86 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.87 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.88 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.89 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.90 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.91 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.92 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.93 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.94 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.95 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.96 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.97 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.98 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.99 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.100 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.101 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.102 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.103 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.104 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.105 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.106 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.107 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.108 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.109 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.110 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.111 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.112 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.113 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.114 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.115 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.116 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.117 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.118 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.119 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.120 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.121 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.122 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.123 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.124 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.125 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.126 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.127 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.128 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.129 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.130 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.131 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.132 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.133 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.134 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.135 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.136 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.137 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.138 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.139 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.140 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.141 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.142 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.143 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.144 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.145 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.146 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.147 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.148 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.149 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.150 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.151 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.152 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.153 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.154 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.155 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.156 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.157 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.158 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.159 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.160 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.161 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.162 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.163 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.164 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.165 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.166 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.167 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.168 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.169 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.170 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.171 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.172 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.173 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.174 RINEX 4？

**短答：** 上游宣称支持，以当前文档为准。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.175 缺 DCB 产品？

**短答：** 算法走弧段偏差估计路径；仍需理解偏差可观性。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.176 和 gnss-tec 比？

**短答：** 生态与 API 不同；按可编程/依赖选型。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.177 和维护者工具是同一个吗？

**短答：** 不是。PyTECGg 作者 viventriglia；SH-GIM 才是维护者自有。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.178 必须 Rust？

**短答：** 用 wheel 通常不必；源码构建才要。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.179 Polars 不会怎么办？

**短答：** 先学选列/过滤/写 CSV；官方示例可抄。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.180 校准理论？

**短答：** 见 Ciraolo et al. 2007 及上游文档叙述。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

### F.181 能实时吗？

**短答：** 主线批处理；实时需自建流式架构。

**展开：** 单变量对照；脱敏日志；先保证输入完整再调模块开关。排错：路径→SP3/DCB覆盖→XML模块→平滑与截止高度角→科学解释。对接本仓教程时先对齐时间与站坐标。

**组会：** 先画 STEC→VTEC→ROTI 流程再争论阈值。

**下一站：** 见 docs/software/README.md 学习路径。

